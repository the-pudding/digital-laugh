<section id='outro'>
	<div class='prose notes'>
		{{#each doc.notes}}
		<p>{{{value}}}</p>
		{{/each}}
	</div>

	<div class='prose method'>
		{{#each doc.method}}
		<p>{{{value}}}</p>
		{{/each}}
	</div>
</section>