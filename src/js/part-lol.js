/* global d3 */

const $section = d3.select('#lol');
const $content = $section.select('.section__content');

function resize() {
  if ($content.size()) {
  }
}

function init() {
  if ($content.size()) {
  }
}

export default { init, resize };
